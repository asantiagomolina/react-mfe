import React from "react"
import Navbar from "./NavBar";

const Header = () => {
    return (
        <header className="flex text-white font-bold text-lg">
            <Navbar />
        </header>
    );
}

export default Header;
